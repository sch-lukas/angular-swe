meta {
  name: Buch mit ID 60 löschen
  type: graphql
  seq: 8
}

post {
  url: {{graphqlUrl}}
  body: graphql
  auth: inherit
}

headers {
  Accept: application/graphql-response+json
}

body:graphql {
  mutation {
    delete(id: "60") {
        success
    }
  }
}

assert {
  res.status: eq 200
  res.headers['content-type']: matches ^application\/graphql-response\+json(; charset=utf-8)?$
  res.body.data.delete.success: isBoolean
  res.body.data.delete.success: isTruthy
}

settings {
  encodeUrl: true
  timeout: 0
}
