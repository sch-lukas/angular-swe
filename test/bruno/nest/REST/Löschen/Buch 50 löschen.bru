meta {
  name: Buch 50 löschen
  type: http
  seq: 1
}

delete {
  url: {{restUrl}}/:id
  body: json
  auth: bearer
}

params:path {
  id: 50
}

auth:bearer {
  token: {{TOKEN}}
}

assert {
  res.status: eq 204
  res.body: isString
  res.body: isEmpty
}

settings {
  encodeUrl: true
  timeout: 0
}
