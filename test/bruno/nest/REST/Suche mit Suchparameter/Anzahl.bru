meta {
  name: Anzahl
  type: http
  seq: 23
}

get {
  url: {{restUrl}}?only=count
  body: none
  auth: inherit
}

params:query {
  only: count
}

assert {
  res.status: eq 200
  res.headers['content-type']: matches ^application\/json(; charset=utf-8)?$
  res.body.count: gte 0
}

settings {
  encodeUrl: true
  timeout: 0
}
