meta {
  name: Alle Seite 2
  type: http
  seq: 2
}

get {
  url: {{restUrl}}?page=2
  body: none
  auth: inherit
}

params:query {
  page: 2
}

headers {
  Accept: application/json
}

assert {
  res.status: eq 200
  res.headers['content-type']: matches ^application\/json(; charset=utf-8)?$
  res.body.content: isArray
  res.body.content: isNotEmpty
  res.body.page.number: eq 1
}

tests {
  test('Body: mind. 1 Buch und ID', () => {
    res.getBody()
      .content
      .map(buch => buch.id)
        .forEach(id => expect(id).to.be.above(0));
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
