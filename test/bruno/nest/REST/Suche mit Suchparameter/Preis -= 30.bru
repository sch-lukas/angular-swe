meta {
  name: Preis <= 30
  type: http
  seq: 19
}

get {
  url: {{restUrl}}?preis=30
  body: none
  auth: inherit
}

params:query {
  preis: 30
}

headers {
  Accept: application/json
}

assert {
  res.status: eq 200
  res.headers['content-type']: matches ^application\/json(; charset=utf-8)?$
  res.body.content: isArray
  res.body.content: isNotEmpty
}

tests {
  test('Body: preis und ID', () => {
    res.getBody()
      .content
      .forEach(buch => {
        const { preis, id } = buch;
        expect(Number(preis)).to.be.lte(30);
        expect(id).to.be.above(0);
      });
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
